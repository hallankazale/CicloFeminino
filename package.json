{
  "name": "luna-ciclo-feminino",
  "version": "1.0.0",
  "private": true,
  "description": "Luna — aplicativo de acompanhamento do ciclo feminino para Android.",
  "scripts": {
    "prepare:web": "node scripts/prepare-web.mjs",
    "test": "node --test tests/*.test.mjs",
    "android:add": "npm run prepare:web && npx cap add android",
    "android:sync": "npm run prepare:web && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/core": "8.4.2",
    "@capacitor/local-notifications": "8.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2"
  },
  "engines": {
    "node": ">=20"
  }
}
